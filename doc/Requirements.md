Before deploying anything you should make sure that you have an hosted database on your computer or somewhere else (In our case we used a VPS, the cost was 8 euros/Month).  
The database was running with MariaDB, we had to use an API to display the data and we use an UI API for do some test.

Usefull link : 
```https://swagger.io/tools/swagger-ui/```  
```https://mariadb.org/```  
```https://redheberg.fr/``` <-- The website where we bought the VPS  
 ```https://github.com/FAST-SUSHI-NEXTECH/API``` <-- The code of the API if you need it
